<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Health Record</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
</head>
<body>
    <%- include('../partials/_navbar.ejs') %>
    
    <div class="container-healthData">
        <h1>Edit Personal Health Metrics</h1>
        <form action="/healthData/<%= healthData._id %>?_method=PUT" method="POST" class="health-metrics-form">
          
          <div class="form-group">
            <label for="name">Name</label>
            <input type="text" name="name" id="name" required value="<%= healthData.name %>" />
          </div>
          
          <div class="form-group">
            <label for="age">Age (15-80)</label>
            <input type="number" name="age" id="age" min="15" max="80" required value="<%= healthData.age %>" />
          </div>
          
          <div class="form-group gender-group">
            <label>Gender</label>
            <label for="male">
              <input type="radio" id="male" name="gender" value="male" <%= healthData.gender === 'male' ? 'checked' : '' %> /> Male
            </label>
            <label for="female">
              <input type="radio" id="female" name="gender" value="female" <%= healthData.gender === 'female' ? 'checked' : '' %> /> Female
            </label>
          </div>
      
          <div class="form-group">
            <label for="weight">Weight (kg)</label>
            <input type="number" name="weight" id="weight" min="0" required value="<%= healthData.weight %>" />
          </div>
          
          <div class="form-group">
            <label for="height">Height (cm)</label>
            <input type="number" name="height" id="height" min="0" required value="<%= healthData.height %>" />
          </div>
      
          <div class="form-group">
            <label for="activityLevel">Activity Level</label>
            <select name="activityLevel" id="activityLevel" required>
              <option value="sedentary" <%= healthData.activityLevel === 'sedentary' ? 'selected' : '' %>>Sedentary</option>
              <option value="light" <%= healthData.activityLevel === 'light' ? 'selected' : '' %>>Light</option>
              <option value="moderate" <%= healthData.activityLevel === 'moderate' ? 'selected' : '' %>>Moderate</option>
              <option value="heavy" <%= healthData.activityLevel === 'heavy' ? 'selected' : '' %>>Heavy</option>
              <option value="very heavy" <%= healthData.activityLevel === 'very heavy' ? 'selected' : '' %>>Very Heavy</option>
            </select>
          </div>
      
          <button type="submit" class="submit-btn">Update Health Record</button>
        </form>    
    </div>
    
</body>
</html>
